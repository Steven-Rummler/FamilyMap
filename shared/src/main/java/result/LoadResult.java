package result;

public class LoadResult {
    /**
     * A message describing the success or failure of
     * loading the given data into the database
     */
    public String message;
    /**
     * True if the given data was successfully loaded
     * into the database, false otherwise
     */
    public Boolean success;
}
